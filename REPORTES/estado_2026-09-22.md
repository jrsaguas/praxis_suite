# Reporte de estado — Praxis Suite
Fecha de revisión: 2026-09-23
Rama: `audit/hardening-2026-09`
PR: #1 (draft, abierto, no mergeado a main)
HEAD inspeccionado: `282277899b31ba320fcefd2ab0c7fc6151fa1760`

## Estado ejecutivo

La cadena Runtime → Execution → Evaluation → Experience → Versioning → Snapshot → Historical Preview → Branching → Manifest está implementada y cuenta con pruebas unitarias/de integración relevantes. Durante la verificación final se detectaron dos huecos de cobertura que fueron corregidos de forma localizada: rechazo explícito de traversal en preview histórico y controles completos de retorno/contexto/restauración en la vista histórica.

El CI `35951100534` fue verificado como **success** sobre `baf33161d9ea66183d7e19b2e46cdd316622c0e7`. Los commits posteriores de hardening de pruebas/UI aún no tienen una ejecución de CI observable mediante el conector disponible; por tanto, el bloque **no se declara cerrado todavía**.

## IMPLEMENTADO

- Endpoint `POST /api/agent-graph/execute` cableado con planner, runtime y experience sink.
- Runtime observable con tareas, dependencias, contexto, modelo, retries y trace.
- `final_auditor` seguido de `experience_evaluator`.
- Experiencia persistida como candidate y selección futura restringida a accepted.
- Perfiles de evaluación y contexto de profundidad.
- Promoción de ejecuciones solamente cuando el estado es `ok`.
- Versiones con `parent_version_id`, navegación y snapshots físicos.
- Restauración como nueva versión con actualización de manifest.
- Preview histórico MD/HTML read-only con SHA-256.
- Metadata de snapshot disponible sin hard-codear nombres de artefactos.
- UI histórica con preview, restauración y selección como contexto.
- Snapshot de la versión raíz en `save_response_to_chat()`.
- Freshness de derivados y manifest basado en `_ARTIFACT_ROOTS`.
- Pruebas de branching contextual y restauración HTTP.
- Nueva cobertura de rechazo de traversal para lectura histórica.

## VERIFICADO

CI `35951100534`:
- Install dependencies: success
- Compile Python modules: success
- Run security regression tests: success

Pruebas verificadas en el código actual:
- preview histórico read-only
- restore HTTP
- branching desde versión seleccionada
- manifest/freshness
- runtime HTTP wiring
- experience persistence failure no fatal
- auditoría final y evaluación posterior
- model routing
- traversal de preview histórico (añadido en HEAD actual)

## PARCIAL / PENDIENTE

- CI del HEAD actual `282277899b31ba320fcefd2ab0c7fc6151fa1760`: pendiente de una ejecución observable.
- Flujo con datos reales de una investigación/modelo real: no se declara certificado por esta auditoría.
- Feedback de usuario integrado visualmente: fuera del cierre mínimo actual.
- No existe evidencia de que los artefactos externos al snapshot físico deban incorporarse automáticamente.

## COBERTURA DE SNAPSHOT

### Versionado actualmente
`_ARTIFACT_ROOTS` cubre:
- `entregables/documentos`
- `entregables/visualizador_interactivo`
- `entregables/imagenes`
- `entregables/codigo_graficos`
- `proceso_agentes`

Por extensión se tipifican MD, HTML, DOCX, DOC, imágenes, Python/JS/CSS y otros archivos encontrados bajo esas raíces.

### No versionado físicamente
- `conversacion_metadata.json`: permanece como metadata/control de historial; contiene manifest, versiones y registros.
- Elementos fuera de `_ARTIFACT_ROOTS`: no forman parte del snapshot físico actual.
- Configuración global, credenciales, entorno y estado externo: no son artefactos de una versión de investigación.

### Reconstruible
- Derivados como DOCX/DOC/HTML/Canvas/imágenes pueden regenerarse desde los artefactos fuente cuando existe el proceso correspondiente.
- El manifest se recalcula desde el estado físico y no se usa como sustituto del snapshot.

### Externo al snapshot
- Proveedores/modelos LLM, Ollama, RAG remoto y cualquier recurso fuera de la carpeta de respuesta.

## LIMITACIÓN CONOCIDA

El scoring de `experience_evaluator` sigue siendo deliberadamente simple en comparación con un evaluador multidimensional completo. No bloquea el cierre funcional, pero queda como deuda posterior.

## Criterio de cierre

El bloque se declarará cerrado únicamente cuando una ejecución de CI verificable corresponda al HEAD actual o a un commit posterior que incluya estos últimos cambios.
